# 有序连点器 2.1.0

## 可靠性

- 执行断点实时落盘，异常退出后可在下次启动恢复。
- 安全角改为后台监控，界面线程繁忙时仍能触发停止。
- 关闭窗口时先停止执行并等待断点保存，避免任务被直接截断。
- 同一数据目录只允许运行一个实例；重复启动会唤醒已有窗口。

## 使用体验

- 执行进度合并为每 100 毫秒刷新一次，长任务界面更稳定。
- “2 秒后记录”支持替换和取消，连续点击不会产生多个延迟采点。
- 草稿仅在内容变化后保存，减少无效磁盘写入。
- 执行日志和诊断日志自动按 30 天、1000 个文件、100 MB 上限清理。

## 兼容性

- `.oclick` 方案格式仍为 v4，与 2.0.0 方案兼容。
- 支持 Windows 10/11 x64。
- 安装版继续使用当前用户安装，不要求管理员权限。
